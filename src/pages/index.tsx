import React from 'react';
import Header from '@/components/header';
import type {NextPage} from 'next';

const Home: NextPage = () => {
  return (
    <div>
      <Header></Header>
    </div>
  );
};

export default Home;
